import { makeStyles } from "@material-ui/core/styles";

const groupsPageStyle = makeStyles(() => ({
  GroupsPageSize: {
    float: "right",
    width: "70%",
    marginRight: "10%",
    marginTop: "6%",
  },
}));

export default groupsPageStyle;