version: "3"
services:
  geoProteccion:
    container_name: geo-proteccion-container
    build: .
    image: geo-proteccion
    ports:
      - "8888:8888"
    volumes:
      - "../build:/usr/share/nginx/html"
